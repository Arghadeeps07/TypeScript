const user = {
    name: "Arghadeep",
    email: "arghadeeps07@gmail.com",
    date : Date.now()
}

console.log(user);


// In this type of cases cases we are ok without using the :datatype type of format
let n = 7
//n = "abc" // as typescript is smart enough a automatically ditects the type of the variable

let a: string // in this cases its recomended to add the type to not get the "any" 

a = "Hello"



export{}
